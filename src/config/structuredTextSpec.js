/**
 * AI 生成与用户可读长文本的版面约定（对齐工作台「分层列表 + 标签强调」，禁止通排长段）。
 * 渲染侧配合：`CoachFormattedText` + `.coach-md`（Markdown）。
 */

export const STRUCTURED_OUTPUT_RULES_ZH = `
【版面结构（必守·禁止整篇通排长段）】
1. **分层**：用 Markdown 标题分大块——\`## 小节\`、\`### 更细的节\`；禁止只输出一段没有标题的长正文。
2. **并列列表化**：要点用 \`- \` 无序列表；有顺序的步骤用 \`1. 2. \`；并列信息不要用逗号拼成超长一句。
3. **标签扫读**：列表项优先写成「**\`**关键词或维度**\`**：**说明文字」，或「**\`**标签**\`**——**要点」（与 $APPEALS 等维度一致时可写 **$（价格）**：…）。
4. **强调**：结论、数字、对比关系用 **粗体**；出处类型、限定条件可用括号（据公开报道/业内推断等）。
5. **分段**：块与块之间必须空一行（JSON 字符串内写 \\n\\n）。
`.trim()
