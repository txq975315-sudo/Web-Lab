/**
 * 压力测试引擎 — Prompt 与常量占位（实现见 PRD/Agent_压力训练MVP讨论）。
 * 与 `src/features/pressureTest/` 代码域配套；业务逻辑接入时再填充模板。
 */
export const PRESSURE_TEST_PROMPTS_VERSION = '0-placeholder'

/** 与 UI 层 `STORAGE_DEPTH` 一致，便于后续集中改键名时检索 */
export const PRESSURE_TEST_DEPTH_STORAGE_KEY = 'thinking-lab-pressure-depth'
