/**
 * 商业假设构建器 — Prompt 资产
 * 对齐 PRD/Agent_压力训练MVP讨论/
 *   - 压力测试引擎_商业假设构建器版.md
 *   - 商业假设构建器_最优生产级Prompt.md
 */

export const HYPOTHESIS_PROMPTS_VERSION = '1.0.0-mvp'

/** @type {readonly { version: string, date: string, summary: string }[]} */
export const HYPOTHESIS_PROMPT_CHANGELOG = Object.freeze([
  { version: '1.0.0-mvp', date: '2026-05', summary: '初始 MVP：商业教练系统角色、8 问链式引导指令、画布生成指令' },
])

/** Layer2 异步抽检抽样率 */
export const HYPOTHESIS_LAYER2_SAMPLE_RATE = 0.1

/** 商业教练系统角色（来自 PRD §5 / 最优生产级Prompt） */
export const HYPOTHESIS_SYSTEM_PROMPT = `你是一位商业教练。你的目标不是判断用户的想法好不好，而是帮用户把模糊的想法变成清晰的、可执行的商业假设。

你的工作方式：
- 一步一步引导用户想清楚每个维度（用户/痛点/方案/商业模式）
- 每个追问都聚焦一个具体的假设构建任务
- 当用户回答模糊时，不否定，而是给思考框架帮用户展开
- 每个追问结尾都有一句建设性引导
- 追问中自然引用用户上文的回答

你的核心信念：
- 没有坏想法，只有没想清楚的想法
- 每个模糊的想法都可以变成一组可验证的假设
- 你的价值是帮用户"想清楚"和"写下来"，不是"评判"和"打分"

追问原则：
1. 引用原则：引用用户上文的回答，保持对话连贯
2. 共建原则：语气是"我们一起来想清楚"，不是"你回答我问题"
3. 聚焦原则：每个追问只聚焦一个维度，不发散
4. 行动原则：每轮追问结尾都指向"写下来"，最终指向"去做"

语气要求：
- 温和但有方向感，像教练带学员
- 追问长度50-100字
- 追问结尾给用户思考框架提示
- 追问中自然引用用户上文的具体词句

禁忌（绝对不能说）：
- "你的想法有问题"
- "这个不可能"
- "别人已经做了"
- "这没有价值"
- 任何否定性、打压性评价

替代说法：
- "这个想法有一个有趣的切入点——如果要让它更完整，我们还可以想想..."
- "这个方向可以——我们来一起把它写清楚"
- "你刚才说得很具体——如果要从这个点展开，我们还可以..."

思考框架（隐藏输出，不展示给用户）：
步骤1：用户上一问回答了什么？提取关键信息。
步骤2：下一轮需要构建什么假设？明确目标。
步骤3：怎么引导用户把假设写清楚？设计追问。
步骤4：追问结尾怎么指向下一步？给出框架。

输出格式：只输出追问文本，不输出分析过程，不输出思考步骤。`

/**
 * 生成第 (round, question) 追问的用户指令
 * @param {number} round — 1..4
 * @param {number} question — 1..2
 * @param {object} context
 * @param {string} context.originalIdea
 * @param {object} [context.priorQA] — 历史 QA 上下文
 */
export function buildQuestionGenerationUserPrompt(round, question, context) {
  const { originalIdea } = context

  if (round === 1 && question === 1) {
    return `【任务】生成第1轮第1问。目标：帮用户定义清楚"目标用户是谁"。

【当前素材】
用户原始想法：${originalIdea}

【生成要求】
1. 温和引导用户描述目标用户的具体特征
2. 追问结尾给思考框架提示（"可以从人群特征/使用场景来想"）
3. 如果用户描述笼统（如"大学生"），追问帮用户细分
4. 语气："我们先从目标用户开始——"

【输出】只输出追问文本，50-100字。`
  }

  if (round === 1 && question === 2) {
    return `【任务】生成第1轮第2问。目标：帮用户明确"核心痛点是什么"。

【生成要求】
1. 引用用户上一问对目标用户的定义
2. 引导用户描述"在什么场景下遇到什么问题"
3. 追问结尾提示"痛点越具体越好，试着描述一个具体场景"
4. 语气：引导用户展开，不给压力

【输出】只输出追问文本，50-100字。`
  }

  if (round === 2 && question === 1) {
    return `【任务】生成第2轮第1问。目标：帮用户明确"核心功能是什么"。

【生成要求】
1. 引用用户定义的目标用户和痛点
2. 引导用户描述"用什么方式解决这个痛点"
3. 追问结尾提示"聚焦最核心的一个功能，不用面面俱到"
4. 语气："你刚才说得很清楚——"

【输出】只输出追问文本，50-100字。`
  }

  if (round === 2 && question === 2) {
    return `【任务】生成第2轮第2问。目标：帮用户想清楚"和现有方案有什么不同"。

【生成要求】
1. 引用用户描述的核心功能
2. 引导用户思考"和现有解决方案相比，你的不同在哪"
3. 追问结尾提示"不用说你比别人好，只要说清楚不同在哪"
4. 语气：建设性探索，不否定

【输出】只输出追问文本，50-100字。`
  }

  if (round === 3 && question === 1) {
    return `【任务】生成第3轮第1问。目标：帮用户想清楚"怎么收费"。

【生成要求】
1. 引用用户定义的目标用户和解决方案
2. 引导用户思考定价策略（免费增值/订阅/一次性）
3. 追问结尾提示"定价是一个假设，后面我们会一起验证"
4. 语气："我们到了商业模式——"

【输出】只输出追问文本，50-100字。`
  }

  if (round === 3 && question === 2) {
    return `【任务】生成第3轮第2问。目标：帮用户想清楚"怎么获取第一批用户"。

【生成要求】
1. 引用用户描述的目标用户和定价
2. 引导用户思考"第一批100个用户从哪来"
3. 追问结尾提示"如果只能选一个渠道，你会选哪个？为什么？"
4. 语气：聚焦选择

【输出】只输出追问文本，50-100字。`
  }

  if (round === 4 && question === 1) {
    return `【任务】生成第4轮第1问。目标：帮用户识别"最关键的需要验证的假设"。

【生成要求】
1. 引用用户前3轮的关键回答
2. 帮用户从所有假设中识别"如果错了，项目就垮了"的那个
3. 追问结尾提示"这个假设就是我们验证计划的起点"
4. 语气："我们来盘点一下——"

【输出】只输出追问文本，50-100字。`
  }

  if (round === 4 && question === 2) {
    return `【任务】生成第4轮第2问，全部追问的最后一问。目标：帮用户制定"最小成本的验证方法"。

【生成要求】
1. 引用用户识别的最关键假设
2. 引导用户思考"最快、最省钱的验证方法"
3. 追问结尾过渡到画布生成："接下来我帮你把所有假设整合成一份画布和验证计划。"
4. 语气：指向行动

【输出】追问文本 + 结束语，80-120字。`
  }

  return `请生成第${round}轮第${question}问的追问。`
}

/**
 * @param {string} baseUser — 原指令
 * @param {string} hint — 修正提示
 */
export function buildQuestionRetryUserPrompt(baseUser, hint) {
  return `${baseUser}

【上次未通过质检】
原因：${hint}
请根据以上要求重新生成追问。`
}

/**
 * 画布生成指令（8 问全部回答完毕后调用）
 * @param {string} allAnswersJson — collectAllAnswers() 的 JSON 字符串
 */
export function buildCanvasGenerationUserPrompt(allAnswersJson) {
  return `【任务】基于用户4轮追问的全部回答，生成商业假设画布和验证计划。
不要输出任何前缀后缀，直接输出以下格式的 Markdown。确保所有字段完整填写。

用户回答汇总：
${allAnswersJson}

请按以下格式输出：

## 你的商业假设画布

### 1. 用户假设
- 目标用户：
- 使用场景：
- 核心痛点：

### 2. 解决方案假设
- 核心功能：
- 差异化：
- 使用流程：

### 3. 商业模式假设
- 定价策略：
- 获客渠道：
- 收入预测：（用户数量 × 单价 = 月收入）

### 4. 验证计划
| 优先级 | 假设 | 验证方法 | 时间 | 成功标准 |
|--------|------|---------|------|---------|
| 🔴 P0 | （最关键假设） | （最小成本方法） | 1周 | （可量化指标） |
| 🟡 P1 | （次要假设1） | （验证方法） | 2周 | （成功标准） |
| 🟢 P2 | （次要假设2） | （验证方法） | 2周 | （成功标准） |

### 下一步行动
本周最高优先级：（具体到本周可执行的一件事）

生成要求：
1. 所有字段必须填写完整，不要留空
2. 收入预测要给出具体数字
3. 验证计划的成功标准要可量化（百分比/数量）
4. 下一步行动要具体到"做什么、在哪里做、用什么工具"`
}

/** 8 个预设 Fallback 追问模板 */
export const FALLBACK_QUESTIONS = [
  // R1Q1
  "我们先从目标用户开始——你说的是一个比较大的群体。能不能再具体一些？比如他们的年龄、职业、日常使用场景？我们从这些维度来把目标用户定义得更清楚。",
  // R1Q2
  "你刚才说了目标用户——他们在什么场景下遇到了什么困扰？试着描述一个具体的画面，比如'一个用户在某天遇到了……'，痛点越具体越好。",
  // R2Q1
  "基于你描述的用户和痛点——你打算用什么方式帮他们解决？先聚焦最核心的一个功能就可以，不需要面面俱到。",
  // R2Q2
  "你刚才说的核心功能——现在市面上有没有类似的产品？你的方案和它们有什么本质的不同？不用说你比他们好，只要说清楚不同在哪。",
  // R3Q1
  "我们到了商业模式——这个方案你打算怎么收费？免费增值、订阅制还是一次性付费？先给一个初步假设，后面我们会一起验证。",
  // R3Q2
  "你说定了收费方式——那第一批用户从哪来？如果只能选一个获客渠道，你选哪个？为什么觉得这个渠道最有效？",
  // R4Q1
  "我们来盘点一下前几轮的关键假设——如果只能选一个来验证，哪一个假设错了你的整个项目就站不住了？这个假设就是我们验证计划的起点。",
  // R4Q2
  "你说出了最关键的那个假设——要验证它，最快、最省钱的方法是什么？设计一份问卷？做一个落地页？还是直接找目标用户聊一聊？选一个本周就能开始的方法。",
]

/** 质检配置常量 */
export const QC_RULES = Object.freeze({
  MIN_LENGTH: 30,
  MAX_LENGTH: 120,
  FORBIDDEN_WORDS: ['有问题', '不可能', '已经做了', '没有价值', '不行', '这没有价值'],
  CO_BUILD_KEYWORDS: ['我们', '一起', '来'],
})
