将用作 Thinking Lab 右侧面板背景的 JPG / PNG / WebP 放入本文件夹，
然后在 src/config/labBackgrounds.js 的 LAB_BACKGROUND_IMAGES 数组里追加对应路径，例如：
  '/backgrounds/my-photo.jpg'

保存后刷新页面；轮换节奏默认每小时切换一张（可在 labBackgrounds.js 里改为按天）。
