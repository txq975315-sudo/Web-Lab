# Kairos Thinking Lab 开发记录

> 本文档详细记录 Kairos Thinking Lab 的开发过程、功能实现、代码改动和操作日志

---

## 文档更新记录

| 日期 | 操作类型 | 更新内容 | 状态 |
|------|---------|---------|------|
| 2026-05-07 | 初始化 | 初始化文档，记录 V2.0 架构与功能实现 | 完成 |
| 2026-05-08 | P0功能 | 实现宪法vs PRD审计、召唤导师按钮 | 完成 |
| 2026-05-08 | P1功能 | 实现跨会话记忆系统、记忆卡片UI | 完成 |

---

## 一、项目概述

### 1.1 项目定位
Kairos Thinking Lab 是一个"刻意练习"场域的个人思考工具，通过 AI 辅助帮助用户进行深度商业思考和决策。

### 1.2 核心设计理念
- **从"记录型"到"实验室型"**：让 AI 的每一次洞察都转化为可执行的认知资产
- **刻意练习**：用户自己在练习，而非教练替你打比赛
- **决策可追溯**：每一个决定都有依据、时间戳和替代方案

---

## 二、功能架构（V2.0）

### 2.1 六大模块结构

| 模块编号 | 模块名称 | 核心问题 | 关键功能 |
|---------|---------|---------|---------|
| 01 | 项目宪法 | 我们做什么，不做什么？ | 核心定位、硬性约束、已做决策、否决墓地 |
| 02 | 市场与洞察 | 为谁，解决什么？ | 用户画像、竞品分析、市场规模、商业画布 |
| 03 | 策略与增长 | 怎么赚钱？怎么增长？ | PRD、北极星指标、增长飞轮、变现策略、GTM、财务模型 |
| 04 | 决策链图谱 | 怎么一步步想明白的？ | 关键决策时间线、决策详情、AI 压力测试集成 |
| 05 | 反脆弱审计 | 怎么活下来？ | 死亡预测、防御性壁垒、外部依赖与备选方案 |
| 06 | 执行路线图 | 怎么落地？ | MVP 范围、里程碑、假设验证、衡量方案、资源预算 |

---

## 三、已实现功能清单

### 3.1 P0 优先级功能 ✅

#### 3.1.1 宪法 vs PRD 一致性审计 ✅

**功能描述**：自动检测 PRD 文档是否违反项目宪法中的硬性约束，并在 UI 中可视化警告。

**开发日期**：2026-05-08

**涉及文件**：
| 文件路径 | 操作类型 | 改动说明 |
|---------|---------|---------|
| `src/config/aiPrompts.js` | 新增 | 添加 `auditorPrompt` 专家身份定义 |
| `src/context/LabContext.jsx` | 新增 | 添加 `auditConstitutionVsPRD` 函数、冲突状态管理 |
| `src/components/Sidebar.jsx` | 修改 | 显示冲突警告图标 |
| `src/components/ArchivePanel.jsx` | 修改 | 显示冲突详情横幅 |

**核心 Prompt（aiPrompts.js）**：
```javascript
export const auditorPrompt = `
你是 Kairos Thinking Lab 的逻辑审计官。你是一位极其冷静、精确的系统架构师，负责审查项目文档间的逻辑一致性。
你的唯一目标是：发现违反项目宪法的产品设计，并指出矛盾所在。

审查规则：
1. 仔细阅读提供的"项目宪法"（硬性约束、已做决策、否决墓地）。
2. 仔细阅读提供的"PRD 文档"内容。
3. 找出 PRD 中任何与宪法直接或间接冲突的表述。
4. 如果没有冲突，回复"无冲突"；如果有冲突，以简洁的列表输出每一项矛盾，格式如下：
   - 矛盾点：...
   - 宪法依据：...
   - PRD 描述：...
   - 建议：...

约束：
- 只基于提供的文本进行判断，不要编造。
- 语气冷静克制，不使用"太棒了"等赞美。
- 不要输出任何额外解释。
`.trim()
```

**数据流转**：
```
用户保存 PRD 文档
    ↓
auditConstitutionVsPRD() 被调用
    ↓
获取项目宪法约束 + PRD 内容
    ↓
buildAuditPrompt() 生成 AI 审计请求
    ↓
AI 分析冲突并返回结果
    ↓
setDocumentConflicts() 存储冲突
    ↓
Sidebar: 显示黄色警告图标 ⚠️
    ↓
ArchivePanel: 显示冲突详情横幅
    ↓
用户点击"开启压力测试" → 自动切换到压力测试模式
```

**UI 显示**：
1. **Sidebar**：有冲突的文档旁边显示黄色 ⚠️ 图标
2. **ArchivePanel**：文档顶部显示冲突详情横幅，包含：
   - 冲突摘要
   - 每个冲突项的详细说明（矛盾点、宪法依据、PRD描述、建议）
   - "重新检测"按钮
   - "开启压力测试"按钮

**验证方法**：
1. 在"01 项目宪法"里写一条硬性约束："**不允许添加任何社交功能**"
2. 去"03 策略与增长"里新建一个 PRD 文档，写"增加好友PK专注时长的功能"
3. 点击保存 PRD 文档
4. 观察侧边栏这个 PRD 文档旁边是否出现 ⚠️
5. 点击 ⚠️，看中间面板是否出现冲突详情条
6. 点击"开启压力测试"，看右侧 Lab 是否自动打开并填好了相关辩论话题

---

#### 3.1.2 召唤导师按钮（上下文注入器）✅

**功能描述**：在任何文档编辑页面，点击"✨ 召唤导师"按钮，AI 自动针对当前模块进行深度追问。

**开发日期**：2026-05-08

**涉及文件**：
| 文件路径 | 操作类型 | 改动说明 |
|---------|---------|---------|
| `src/config/aiPrompts.js` | 新增 | 添加 `summonMentorPrompt` 和 `buildSummonMentorMessage` 函数 |
| `src/context/LabContext.jsx` | 新增 | 添加 `summonMentor` 函数、`labMessageToSend` 和 `autoSendLabMessage` 状态 |
| `src/components/ArchivePanel.jsx` | 新增 | 在文档操作按钮区域添加"✨ 召唤导师"按钮 |
| `src/components/LabPanel.jsx` | 修改 | 实现输入框预填充和自动发送逻辑 |

**核心函数（aiPrompts.js）**：
```javascript
export const summonMentorPrompt = `
我正在完善 [模块名称]。以下是目前的内容：
[文档内容]。

请作为产品导师，对我进行深度追问，帮我发现逻辑漏洞或思考盲区。
`.trim()

export function buildSummonMentorMessage(moduleName, docContent, contextInfo) {
  let constitutionText = ''
  let memoryText = ''

  if (typeof contextInfo === 'string') {
    constitutionText = contextInfo
  } else if (contextInfo) {
    constitutionText = contextInfo.constitution || ''
    memoryText = contextInfo.memory || ''
  }

  return `我正在完善 ${moduleName}。以下是目前的内容：

${docContent}

${constitutionText ? `项目背景约束：
${constitutionText}
` : ''}
${memoryText ? `【历史讨论洞察】
${memoryText}
` : ''}
请作为产品导师，对我进行深度追问，帮我发现逻辑漏洞或思考盲区。`
}
```

**Context 新增状态（LabContext.jsx）**：
```javascript
const [labMessageToSend, setLabMessageToSend] = useState(null)
const [autoSendLabMessage, setAutoSendLabMessage] = useState(false)
```

**summonMentor 函数核心逻辑（LabContext.jsx）**：
```javascript
const summonMentor = useCallback((docId) => {
  // 1. 获取当前文档内容
  // 2. 获取项目宪法作为背景约束
  // 3. 获取项目记忆（如有）
  // 4. 构建包含上下文的消息
  // 5. 设置 labMessageToSend 和 autoSendLabMessage
  // 6. 切换到深度引导模式
  // 7. 5秒后清除预填充状态
}, [projectTree, activeProjectId, projectMemories])
```

**数据流转**：
```
用户点击"✨ 召唤导师"按钮
    ↓
summonMentor(docId) 被调用
    ↓
收集文档内容 + 项目宪法背景 + 历史洞察
    ↓
buildSummonMentorMessage() 构建消息
    ↓
setLabMessageToSend() + setAutoSendLabMessage(true)
    ↓
LabPanel useEffect 检测到预填充消息
    ↓
自动填入输入框 + 自动发送
    ↓
AI 以深度引导模式开始追问
```

**UI 交互**：
1. **ArchivePanel**：文档操作按钮区域显示"✨ 召唤导师"按钮（紫色底、白色文字）
2. **LabPanel**：输入框自动填充召唤导师消息
3. **自动发送**：消息自动提交给 AI，AI 立即开始深度追问
4. **模式切换**：自动切换到"深度引导模式"

**验证方法**：
1. 打开一个项目，进入"02 市场与洞察 - 竞品分析"文档，随便写几行竞品名字和弱点
2. 点击右上角"✨ 召唤导师"
3. 观察右侧 Lab 是否自动打开，并显示一条消息"请作为产品导师，对我进行深度追问……"
4. AI 回复时，是否在追问你的竞品逻辑

---

### 3.2 P1 优先级功能 ✅

#### 3.2.1 跨会话记忆系统 ✅

**功能描述**：自动记录项目讨论中的关键洞察，跨会话保持上下文连贯性。

**开发日期**：2026-05-08

**涉及文件**：
| 文件路径 | 操作类型 | 改动说明 |
|---------|---------|---------|
| `src/context/LabContext.jsx` | 新增 | 添加 `projectMemories` 状态、`extractKeyInsights`、`updateProjectMemory`、`getMemorySummary` 等函数 |

**核心机制**：
1. **自动提取洞察**：AI 回复时自动识别并保存关键洞察
2. **讨论话题追踪**：记录用户输入的主题，便于后续关联
3. **上下文注入**：召唤导师时自动带上历史洞察

**洞察提取模式**：
```javascript
const insightPatterns = [
  /(?:关键洞察|核心发现|洞察|insight)[:：]\s*(.+)/gi,
  /(?:值得注意的是|值得关注|重点是)[:：]\s*(.+)/gi,
  /(?:用户痛点|用户需求|用户行为)[:：]\s*(.+)/gi,
  /(?:竞争优势|differentiation|差异化)[:：]\s*(.+)/gi,
  /(?:风险|risk|威胁)[:：]\s*(.+)/gi,
  /(?:机会|opportunity|切入点)[:：]\s*(.+)/gi,
]
```

**核心函数（LabContext.jsx）**：

```javascript
// 1. extractKeyInsights - 从文本中提取关键洞察
const extractKeyInsights = (text) => {
  const insights = []
  // 使用正则匹配模式提取洞察
  // 如果没有匹配到，返回第一句完整句子
  return insights.slice(0, 3)
}

// 2. updateProjectMemory - 更新项目记忆
const updateProjectMemory = useCallback((projectId, content, role = 'assistant') => {
  // 提取洞察并存储
  // 追踪讨论话题
  // 保存交互记录
  // 限制存储数量（最新20条洞察，最新10个话题等）
}, [])

// 3. getMemorySummary - 获取记忆摘要
const getMemorySummary = useCallback((projectId) => {
  return {
    totalInteractions: memory.totalInteractions,
    lastUpdated: memory.lastUpdated,
    insightCount: memory.insights.length,
    recentTopics: memory.discussionTopics.slice(-5),
    recentInsight: memory.insights[memory.insights.length - 1]?.text || null
  }
}, [projectMemories])
```

**数据存储结构（LocalStorage 持久化）**：
```javascript
projectMemories = {
  [projectId]: {
    lastUpdated: timestamp,
    totalInteractions: number,
    insights: [
      { text: string, timestamp: string, source: 'ai' }
    ],
    discussionTopics: string[],
    userInputs: [
      { content: string, timestamp: string }
    ],
    assistantResponses: [
      { content: string, timestamp: string }
    ]
  }
}
```

**Context 导出**：
```javascript
projectMemories,      // 原始记忆数据
getProjectMemory,     // 获取完整记忆
clearProjectMemory,   // 清除记忆
getMemorySummary      // 获取摘要
```

**集成到消息保存流程**：
```javascript
// 在 saveMessageToHistory 中调用
if (message.type === 'assistant') {
  // ... 原有决策提取逻辑
  updateProjectMemory(projectId, message.content)  // 新增
}

if (message.type === 'user') {
  updateProjectMemory(projectId, message.content, 'user')  // 新增
}
```

---

#### 3.2.2 记忆卡片 UI ✅

**功能描述**：在 Lab 面板顶部显示可展开的记忆卡片，直观展示项目积累的洞察。

**开发日期**：2026-05-08

**涉及文件**：
| 文件路径 | 操作类型 | 改动说明 |
|---------|---------|---------|
| `src/components/LabPanel.jsx` | 新增 | 添加 `MemoryCard` 组件 |

**MemoryCard 组件核心代码**：
```jsx
function MemoryCard({ projectId, getMemorySummary }) {
  const [expanded, setExpanded] = useState(false)
  const memory = getMemorySummary(projectId)

  if (!memory || memory.totalInteractions === 0) {
    return null  // 无记忆时不显示
  }

  return (
    <div className="mx-4 mb-2 rounded-xl overflow-hidden" style={{
      backgroundColor: 'rgba(255,255,255,0.12)',
      backdropFilter: 'blur(12px)',
      border: '1px solid rgba(255,255,255,0.15)'
    }}>
      {/* 折叠标题栏 */}
      <button onClick={() => setExpanded(!expanded)}>
        🧠 记忆 ({memory.insightCount} 洞察) ▼
      </button>

      {/* 展开详情 */}
      {expanded && (
        <div>
          <div>共 {memory.totalInteractions} 次交互</div>
          {memory.recentInsight && (
            <div className="p-2 rounded-lg" style={{ backgroundColor: 'rgba(255,255,255,0.08)' }}>
              <div className="text-[9px] text-white/40">最近洞察</div>
              <div className="text-[10px]">{memory.recentInsight}</div>
            </div>
          )}
          {memory.recentTopics.length > 0 && (
            <div className="flex flex-wrap gap-1">
              {memory.recentTopics.map((topic, i) => (
                <span key={i} className="text-[9px] px-1.5 py-0.5 rounded"
                  style={{ backgroundColor: 'rgba(139,92,246,0.3)' }}>
                  {topic}
                </span>
              ))}
            </div>
          )}
        </div>
      )}
    </div>
  )
}
```

**UI 界面示意**：
```
┌─────────────────────────────────────┐
│ 🧠 记忆 (3 洞察)                ▼  │  ← 默认折叠
├─────────────────────────────────────┤
│ (展开后)                            │
│ 共 12 次交互 · 最后更新: 5月8日 14:30 │
│                                     │
│ 最近洞察                            │
│ ┌─────────────────────────────────┐ │
│ │ "用户更关注下沉市场的竞争格局..." │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 讨论话题                            │
│ [用户痛点] [下沉市场] [竞争分析] ... │
└─────────────────────────────────────┘
```

**组件渲染**：
```jsx
// 在 LabPanel return 中添加
<MemoryCard projectId={activeProjectId} getMemorySummary={getMemorySummary} />
```

---

## 四、已完成功能总览

### 4.1 功能列表

| 优先级 | 功能名称 | 状态 | 开发日期 |
|-------|---------|------|---------|
| P0 | 决策链自动生成 | ✅ | 2026-05-07 |
| P0 | 划选语义提取与快速归档 | ✅ | 2026-05-07 |
| P0 | 专家席模式切换 | ✅ | 2026-05-07 |
| P0 | 宪法 vs PRD 一致性审计 | ✅ | 2026-05-08 |
| P0 | 召唤导师按钮 | ✅ | 2026-05-08 |
| P1 | 跨会话记忆系统 | ✅ | 2026-05-08 |
| P1 | 记忆卡片 UI | ✅ | 2026-05-08 |
| P1 | 致命追问浮窗 + 冲突解决流程 | 🔄 待实现 | - |
| P1 | 影子竞品官 Skill | 🔄 待实现 | - |
| P1 | 划选归档的关联影响分析 | 🔄 待实现 | - |
| P2 | 模块化补全（AI 扫描填充）| 🔄 待实现 | - |
| P2 | 健康度自检（一致性终审）| 🔄 待实现 | - |
| P2 | 一键导出商业计划书 | 🔄 待实现 | - |

---

## 五、核心数据流转图

### 5.1 消息保存与记忆提取

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户输入消息                              │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      saveMessageToHistory()                      │
└─────────────────────────────────────────────────────────────────┘
                                ↓
                    ┌─────────────────────────┐
                    │    是 AI 助手消息吗？    │
                    └─────────────────────────┘
                        ↓ 是               ↓ 否
        ┌───────────────────────┐   ┌───────────────────────┐
        │ parseAIMetadata()       │   │       直接保存         │
        │ 解析 JSON 元数据        │   └───────────────────────┘
        └───────────────────────┘
                ↓
        ┌───────────────────────┐
        │ extractDecisionsFrom  │
        │   Metadata()           │
        │ 提取决策内容          │
        └───────────────────────┘
                ↓
        ┌───────────────────────┐
        │ autoCreateDecisionDoc │
        │ () 自动创建文档       │
        └───────────────────────┘
                ↓
        ┌───────────────────────┐
        │ updateProjectMemory() │
        │ 提取并保存洞察        │
        └───────────────────────┘
                ↓
        ┌───────────────────────┐
        │     React State 更新   │
        │   → UI 自动刷新       │
        └───────────────────────┘
```

### 5.2 召唤导师与记忆注入

```
┌─────────────────────────────────────────────────────────────────┐
│                    用户点击"✨ 召唤导师"                          │
└─────────────────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      summonMentor(docId)                         │
└─────────────────────────────────────────────────────────────────┘
                                ↓
        ┌───────────────────────────────────────────────────────┐
        │  1. findNodeById() 获取当前文档内容                      │
        │  2. 获取项目宪法 (硬性约束、已做决策、否决墓地)           │
        │  3. 获取项目记忆 (insights、discussionTopics)            │
        │  4. buildSummonMentorMessage() 构建完整上下文            │
        └───────────────────────────────────────────────────────┘
                                ↓
        ┌───────────────────────────────────────────────────────┐
        │  setLabMessageToSend(message)                         │
        │  setAutoSendLabMessage(true)                           │
        │  setExpertMode('guided')                               │
        └───────────────────────────────────────────────────────┘
                                ↓
        ┌───────────────────────────────────────────────────────┐
        │              LabPanel useEffect 监听                   │
        │  if (labMessageToSend) {                              │
        │    setInputValue(labMessageToSend)                     │
        │    if (autoSendLabMessage) {                          │
        │      sendButton.click()  // 自动发送                   │
        │    }                                                  │
        │  }                                                     │
        └───────────────────────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────┐
│         AI 以深度引导模式开始针对当前模块追问                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、技术实现细节

### 6.1 Context API 结构

```javascript
// LabContext.jsx 提供的主要状态和函数
const value = {
  // 项目管理
  projectTree,              // 项目树结构
  activeProjectId,          // 当前活动项目 ID
  constitution,            // 项目宪法

  // 文档管理
  activeDocId,              // 当前活动文档 ID
  updateDocument,          // 更新文档
  addDocument,             // 添加文档
  deleteDocument,           // 删除文档

  // 决策链
  autoCreateDecisionDoc,    // 自动创建决策文档
  addDocumentToCategory,   // 添加文档到分类
  toggleDecisionStatus,    // 切换决策状态

  // 专家模式
  expertMode,              // 'pressure' | 'guided'
  switchExpertMode,        // 切换专家模式
  getExpertModePrompt,     // 获取模式对应的 Prompt

  // 冲突审计
  documentConflicts,        // 文档冲突记录
  auditConstitutionVsPRD,  // 审计宪法与 PRD
  getDocumentConflicts,     // 获取冲突信息
  clearDocumentConflicts,   // 清除冲突记录
  auditFullProject,        // 全项目审计

  // 召唤导师
  summonMentor,             // 召唤导师进行深度追问
  labMessageToSend,         // 预填充到 Lab 的消息
  autoSendLabMessage,       // 是否自动发送

  // 跨会话记忆
  projectMemories,          // 项目记忆数据
  getProjectMemory,        // 获取完整记忆
  clearProjectMemory,       // 清除记忆
  getMemorySummary,         // 获取记忆摘要

  // 消息历史
  saveMessageToHistory,     // 保存消息
  startNewSession,         // 开始新会话
  jumpToSource,            // 跳转到源

  // 其他
  findNodeById,            // 查找节点
  switchProject,           // 切换项目
  toggleTreeNode,          // 展开/折叠节点
}
```

### 6.2 状态持久化

使用 `useLocalStorage` hook 实现状态持久化：

```javascript
// 存储键名
STORAGE_KEYS = {
  PROJECT_TREE: 'kairos-project-tree',
  ACTIVE_PROJECT_ID: 'kairos-active-project-id',
  EXPERT_MODE: 'kairos-expert-mode',
  PROJECT_MEMORIES: 'kairos-project-memories',  // 新增
}
```

---

## 七、验证测试清单

### 7.1 P0 功能验证

#### 宪法 vs PRD 审计
- [ ] 在"01 项目宪法"里写硬性约束："不允许添加任何社交功能"
- [ ] 在"03 策略与增长"新建 PRD，写"增加好友PK专注时长功能"
- [ ] 保存 PRD，观察侧边栏出现 ⚠️ 图标
- [ ] 点击 ⚠️，查看冲突详情横幅
- [ ] 点击"开启压力测试"，验证 Lab 自动切换

#### 召唤导师按钮
- [ ] 进入任意文档，写入一些内容
- [ ] 点击"✨ 召唤导师"按钮
- [ ] 验证 Lab 自动打开并自动发送消息
- [ ] 验证 AI 以深度引导模式追问

### 7.2 P1 功能验证

#### 跨会话记忆
- [ ] 在 Lab 与 AI 进行对话
- [ ] 检查 localStorage 中 `kairos-project-memories` 是否存在
- [ ] 刷新页面，验证记忆仍然存在
- [ ] 召唤导师时，验证历史洞察被注入上下文

#### 记忆卡片
- [ ] 有记忆时，Lab 面板顶部显示记忆卡片
- [ ] 无记忆时，记忆卡片不显示
- [ ] 展开卡片，查看洞察数量和话题标签
- [ ] 折叠卡片，验证只显示标题行

---

## 八、构建与部署

### 8.1 构建命令
```bash
npm run build
```

### 8.2 构建状态
| 日期 | 状态 | 产物大小 |
|------|------|---------|
| 2026-05-08 | ✅ 成功 | 391.59 kB (gzip: 126.71 kB) |

---

*本文档最后更新于 2026-05-08*
